namespace Zaj2.Properties
{
    public class LiquidContainer : Container
    {
        protected LiquidContainer(double cargoMass, double height, double emptyWeight, double depth, double maxCapacity, string serialNumber)
            : base(cargoMass, height, emptyWeight, depth, maxCapacity, serialNumber)
        {
            
        }
    }
}